{% render 'breadcrumb' %}

<div class="bg-white dark:bg-gray-900 py-8 md:py-12">
  <div class="container mx-auto px-4">
    <div class="text-center mb-8">
      <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900 dark:text-white mb-4">{{ page.title }}</h1>
      <div class="max-w-3xl mx-auto text-gray-600 dark:text-gray-400">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>

{% section 'flash-deals' %}
{% section 'newsletter-signup' %}