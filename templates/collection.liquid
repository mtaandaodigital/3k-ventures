<div class="container mx-auto px-4 py-8">
  <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
    {% if section.settings.enable_sidebar %}
      <div class="md:col-span-1">
        {% section 'collection-sidebar' %}
      </div>

      <div class="md:col-span-3">
    {% else %}
      <div class="md:col-span-4">
    {% endif %}

        {% if section.settings.show_collection_image and collection.featured_image %}
          <img src="{{ collection.featured_image | img_url: 'master' }}" alt="{{ collection.title | escape }}" class="rounded-lg mb-4">
        {% endif %}

        <h1 class="section-title">{{ collection.title }}</h1>
        <p class="text-gray-700 dark:text-gray-300">{{ collection.description }}</p>

        {% render 'product-listing' %}
      </div>
  </div>

  {% render 'collection-schema' %}
</div>

{% schema %}
{
  "name": "Collection",
  "settings": [
    {
      "id": "enable_sidebar",
      "type": "checkbox",
      "label": "Enable Sidebar",
      "default": true
    },
    {
      "id": "view_mode",
      "type": "select",
      "label": "View Mode",
      "default": "grid",
      "options": [
        {
          "value": "grid",
          "label": "Grid"
        },
        {,,
          "value": "list", { {
          "label": "List"     "id": "products_per_page",     "id": "products_per_page",
        }select",select",
      ]      "label": "Products per Page",      "label": "Products per Page",











{% endschema %}}  ]    }      "default": true      "label": "Show Collection Image",      "type": "checkbox",      "id": "show_collection_image",    {    },



















































{% endschema %}}  ]    }      ]        }          "label": "16:9 (Wide)"          "value": "16x9",        {        },          "label": "3:4 (Portrait)"          "value": "3x4",        {        },          "label": "4:3 (Landscape)"          "value": "4x3",        {        },          "label": "1:1 (Square)"          "value": "1x1",        {      "options": [      "default": "1x1",      "label": "Image Aspect Ratio",      "type": "select",      "id": "image_aspect_ratio",    {    },      ]        }          "label": "24"          "value": "24",        {        },          "label": "20"          "value": "20",        {        },          "label": "16"          "value": "16",        {        },          "label": "12"          "value": "12",        {        },          "label": "8"          "value": "8",        {      "options": [      "default": "12",      "default": "12",
      "options": [
        {
          "value": "8",
          "label": "8"
        },
        {
          "value": "12",
          "label": "12"
        },
        {
          "value": "16",
          "label": "16"
        },
        {
          "value": "20",
          "label": "20"
        },
        {
          "value": "24",
          "label": "24"
        }
      ]
    }
  ]
}
{% endschema %}
